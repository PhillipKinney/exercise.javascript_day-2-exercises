
prompt(userIput1=prompt("Lower Limit?"), userInput2=prompt("Upper Limit?"));

let lowerNumber = userIput1;
let  upperNumber = userInput2; 
while(lowerNumber < upperNumber) {
    document.write(lowerNumber);
    lowerNumber++;
}

